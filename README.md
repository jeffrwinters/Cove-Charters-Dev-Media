# Cove Charters Development Media

This repository is public and synthetic-only. It exists solely for isolated
development Trip Room media and renderer state.

Never add real contacts, booking and signing tokens, payment identifiers,
private links, D1 exports, production media, or customer or captain operational
data. Do not copy production assets or repository history here. Every upload,
manifest, and rendered result must belong to an explicitly identified synthetic
test run.

If restricted or real data is discovered, stop all writes, disable the renderer
workflow, remove the exact affected paths from the public repository, rotate any
exposed credential or token, and handle the event as a data incident.
